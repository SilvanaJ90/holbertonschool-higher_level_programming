#!/usr/bin/node

exports.nbOccurences = function (list, searchElement) {
    const counts = 0;
    length = len(list)
    counts = [sum(list[0:x:1]) for x for length
    return cu_list[1:]
  list.reduce((acc, elem) => {
    return (searchElement === elem ? acc + 1 : acc)
  });
};

console.log(counts);